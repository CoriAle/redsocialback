import { CantidadletrasPipe } from './cantidadletras.pipe';

describe('CantidadletrasPipe', () => {
  it('create an instance', () => {
    const pipe = new CantidadletrasPipe();
    expect(pipe).toBeTruthy();
  });
});
